/* import React, { memo } from 'react';
export const ChildComponent = memo(({ onClick }) => {
    console.log('ChildComponent rendered');
    return <button onClick={onClick}>Clicca me</button>;
});
 */
